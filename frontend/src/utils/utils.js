export const config = { 
    baseUrl: 'http://130.193.44.59:3000',
    headers: {
      'Accept': 'application/json',
      'Content-Type': 'application/json',
      // eslint-disable-next-line no-undef
      'Authorization': `Bearer ${token}`
    }
}